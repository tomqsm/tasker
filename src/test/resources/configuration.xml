<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<config>
<database>
        <user>tumcyk</user>
        <password>mypass</password>
        <databaseName>testdb</databaseName>
        <serverName>localhost</serverName>
        <portNumber>1527</portNumber>
        <create>true</create>
        <type>client</type>
        <initializeTables>
            <initializeTable>CREATE TABLE chronicle (id INT NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1) CONSTRAINT chron_pk PRIMARY KEY,tag VARCHAR(50) DEFAULT NULL,description VARCHAR(400) DEFAULT NULL,inserted TIMESTAMP NOT NULL WITH DEFAULT CURRENT_TIMESTAMP)</initializeTable>
            <initializeTable>CREATE TABLE comment (id INT NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1) CONSTRAINT comment_pk PRIMARY KEY, chronicleId INT, description VARCHAR(400) DEFAULT NULL, inserted TIMESTAMP DEFAULT CURRENT_TIMESTAMP, CONSTRAINT chronicleId_fk FOREIGN KEY (chronicleId) REFERENCES chronicle (id))</initializeTable>
            <clearTable>delete from comment</clearTable>
            <clearTable>delete from chronicle</clearTable>
        </initializeTables>
    </database>
<properties>
        <color>blue</color>
        <logging>${log.level}</logging>
        <time>Mon Dec 01 17:44:07 CET 2014</time>
    </properties>
</config>
