<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<config>
    <database>
        <user>${db.user}</user>
        <password>${db.password}</password>
        <databaseName>${db.databaseName}</databaseName>
        <serverName>${db.serverName}</serverName>
        <portNumber>${db.port}</portNumber>
        <create>${db.create}</create>
        <type>${db.type}</type>
        <initializeTables>
            CREATE TABLE chronicle (id INT NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1) CONSTRAINT chron_pk PRIMARY KEY,tag VARCHAR(50) DEFAULT NULL,description VARCHAR(400) DEFAULT NULL,inserted TIMESTAMP DEFAULT CURRENT_TIMESTAMP)
        </initializeTables>
    </database>
    <properties>
        <color>blue</color>
        <logging>${log.level}</logging>
        <time>519228237200171</time>
    </properties>
</config>